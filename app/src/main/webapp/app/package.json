{
  "name": "assets",
  "version": "1.0.0",
  "description": "This is package.json for projectMonitor.",
  "main": "index.js",
  "scripts": {
    "clean": "bin/clean.sh",
    "build:js": "bin/build_js.sh",
    "watch:js": "bin/build_js.sh watch",
    "build:sass": "bin/build_sass.sh",
    "watch:sass": "bin/build_sass.sh watch",
    "build:html": "bin/deploy_html.sh",
    "watch:html": "bin/deploy_html.sh watch",
    "build:all": "npm run build:js & npm run build:sass & npm run build:html",
    "watch:all": "npm run watch:js & npm run watch:sass & npm run watch:html"
  },
  "author": "gyamin",
  "license": "ISC",
  "repository": "https://github.com/gyamin/projectMonitor.git",
  "dependencies": {
    "watch": "^1.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-env": "^1.1.8",
    "node-sass": "^4.5.0",
    "watch": "^1.0.1"
  }
}
